# Artifacts — collina

How Collina builds/maintains agent tooling, if and when the newsletter
documents skills or harness internals firsthand. The bulk of that material —
`github.com/mcollina/skills` (11 skills, cross-model benchmarking) — is
deferred to a separate future `new-source` proposal (this Source's
acceptance rider #2); this area is expected to stay sparse until then.
One concept per file; this index lists them, one line each.

- [git-backed-memory-for-coding-agents](./git-backed-memory-for-coding-agents.md) — `pi-self-learning` gives a coding agent durable, git-backed, tiered memory of past mistakes-and-fixes so lessons compound across sessions instead of resetting.
