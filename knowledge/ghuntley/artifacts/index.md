# Artifacts — ghuntley

How Geoff Huntley's agent tooling is built, where his writing shows concrete
structures or conventions (e.g. the bash-loop Ralph implementation). One concept
per file; this index lists them, one line each.

May stay sparse — `ghuntley` skews toward `practices/`. Do not force material
here.

_Empty until the build-out runs._
