# Practices — steveyegge

How Steve Yegge frames working with coding agents: the workflow patterns beads'
agent instructions encode (how agents should pick up, sequence, and close work).
One concept per file; this index lists them, one line each.

May stay sparse — `steveyegge` enters the corpus via `beads`, a tool, so it skews
toward `artifacts/`. Do not force material here.

- `metadata-first-execution-routing.md` — Structured metadata should drive agent execution decisions before natural language
